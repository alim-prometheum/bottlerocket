[package]
name = "rapid7-insight-agent"
version = "0.1.0"
edition = "2018"
publish = false
build = "build.rs"

[lib]
path = "pkg.rs"

[package.metadata.build-package]
releases-url = "https://docs.rapid7.com/insight-agent"

[build-dependencies]
glibc = { path = "../glibc" }
